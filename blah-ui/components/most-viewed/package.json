{
  "dependencies": {
    "clone": "^1.0.2",
    "snabbdom": "^0.5.1",
    "xhr": "^2.2.2"
  },
  "scripts": {
    "build": "browserify index.js --standalone MostViewed | uglifyjs > most-viewed.min.js"
  }
}
