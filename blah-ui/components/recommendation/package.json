{
  "dependencies": {
    "clone": "^1.0.2",
    "debounce": "^1.0.0",
    "snabbdom": "^0.5.1",
    "xhr": "^2.2.2"
  },
  "scripts": {
    "build": "browserify index.js --standalone Recommendation | uglifyjs > recommendation.min.js"
  }
}
