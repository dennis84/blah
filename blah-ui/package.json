{
  "name": "blah-ui",
  "description": "...",
  "dependencies": {
    "bulma": "^0.2.3",
    "clone": "^2.1.0",
    "debounce": "^1.0.0",
    "masonry-layout": "^4.1.1",
    "moment": "^2.17.1",
    "snabbdom": "^0.5.4",
    "xtend": "^4.0.1"
  },
  "devDependencies": {
    "browserify": "~13.1.1",
    "tape": "~4.6.3",
    "zuul": "~3.11.1"
  },
  "scripts": {
    "build-css": "stylus -u nib src/css/index.styl -o assets/css",
    "build-js": "browserify src/js/index.js | uglifyjs > assets/js/index.js",
    "build-copy": "cp node_modules/bulma/css/bulma.css assets/css",
    "build": "npm run build-css && npm run build-js && npm run build-copy",
    "test": "node --harmony test/index.js",
    "travis-test": "./node_modules/zuul/bin/zuul -- test/index.js",
    "zuul": "./node_modules/zuul/bin/zuul --local 3000 -- test/index.js"
  }
}
